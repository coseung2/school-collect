{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum desktop capability for the v2 foundation.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
