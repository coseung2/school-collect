# Public client configuration. Every value here is compiled into the shipped
# application, so it must never contain a server credential.
VITE_API_BASE_URL=http://127.0.0.1:3000
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=
