# 업무 자동화 설계

상태: 첫 수직 슬라이스 구현. 브라우저 DOM 자동입력과 감시는 아직 구현하지 않습니다.

## 목표

교사가 School Collect 안에서 반복 업무를 **사용자 정의 버튼**으로 등록하고, 이후 같은 레시피 모델을 이용해 다음 세 가지 유형으로 확장합니다.

1. 바로가기: 나이스, K-에듀파인, 기안, 품의, 출결 등 등록 화면 열기
2. 자동입력: 정형 화면의 필드나 학생 × 날짜 행렬에 앱 데이터를 결정론적으로 입력
3. 감시: 체험학습 신청, 결재 대기 등 목록의 새 항목을 감지해 앱에 알림

AI는 실행 엔진의 필수 구성요소로 두지 않습니다. 자동 입력과 감시는 DOM 규칙과 검증 가능한 레시피를 기본으로 하고, AI는 이후 "레시피 생성 보조"에만 선택적으로 사용할 수 있습니다.

## 1단계: 사용자 정의 바로가기

이번 변경의 범위입니다.

```text
School Collect
  -> 업무 자동화
  -> 버튼 이름 + 대상 URL 등록
  -> 로컬 레시피 저장
  -> 버튼 실행
  -> 기본 브라우저가 등록 URL을 엶
  -> 브라우저에 이미 존재하는 업무포털/SSO 세션 사용
```

### 저장 데이터

현재 레시피는 다음 정보만 보관합니다.

```json
{
  "id": "local-random-id",
  "kind": "shortcut",
  "name": "기안",
  "targetUrl": "https://example.invalid/path"
}
```

실제 학교 URL, 계정, 학생 정보 같은 값은 저장소에 넣지 않습니다. 위 JSON은 구조 설명용이며 실행 가능한 실제 업무 URL이 아닙니다.

레시피는 서버에 업로드하지 않고 Tauri 앱의 사용자별 로컬 설정 디렉터리에 저장합니다.

저장 파일과 복구 동작:

- `automation-recipes.tsv`에 탭으로 구분해 `id`, `kind`, `name`, `targetUrl`을 기록합니다.
- 저장은 임시 파일에 쓴 뒤 rename으로 교체합니다. 쓰기 도중 중단되어도 기존 파일이 손상되지 않습니다.
- 읽는 중 형식이 어긋난 행을 만나면 그 행을 `automation-recipes.invalid.tsv`로 옮겨 보관하고 나머지 레시피는 그대로 사용합니다. 손상된 행을 먼저 격리한 뒤에만 원본을 정리하므로, 격리에 실패하면 원본을 그대로 둡니다.

### 저장하지 않는 것

- 브라우저 쿠키
- 세션 토큰
- 인증서 또는 인증서 비밀번호
- 업무포털 비밀번호
- 학생/보호자 개인정보
- 서버 credential

URL 자체에 basic-auth credential이 포함된 경우 저장을 거부합니다.

## 2단계: 현재 화면 등록용 브라우저 동반 확장

"현재 화면을 기안으로 등록" 기능은 브라우저 확장이 담당합니다.

권장 구조:

```text
School Collect (Tauri)
  <-> 제한된 로컬 브리지
  <-> Chrome/Edge Extension
  <-> 현재 탭 DOM
```

확장은 브라우저 프로필의 쿠키 값을 School Collect에 복사하지 않습니다. 현재 탭에서 이미 성립한 로그인 세션을 그대로 사용합니다.

브리지의 요구사항:

- 허용된 extension origin만 연결
- 매 실행마다 명시적인 recipe id 사용
- 임의 JavaScript 문자열 실행 금지
- 메시지는 정해진 command schema만 허용
- cookie/token/password 필드 전달 금지
- 로그에서 URL query와 DOM 텍스트를 기본 redaction

## 3단계: 결정론적 자동입력 레시피

자동입력은 좌표 매크로가 아니라 DOM 의미 단위로 구성합니다.

예:

```json
{
  "kind": "table_fill",
  "target": {
    "page": "sports-club-monthly"
  },
  "identity": {
    "rowBy": ["grade", "class", "number", "name"],
    "columnBy": "day"
  },
  "steps": [
    { "op": "find_row" },
    { "op": "find_column" },
    { "op": "focus_intersection" },
    { "op": "set_value", "source": "attendance.status" },
    { "op": "press_key", "key": "Tab" },
    { "op": "set_value", "source": "attendance.note" }
  ],
  "commit": "manual"
}
```

실제 selector와 화면별 식별자는 사용자가 화면을 등록할 때 생성하며 저장소에 하드코딩하지 않습니다.

### 학생 × 날짜 행렬

월별 스포츠클럽/출결과 같은 화면은 아래 순서로 처리합니다.

1. 월/화면 식별
2. 학생 행 식별
3. 날짜 열 식별
4. 교차 셀 포커스
5. 필요한 Tab/Enter 흐름 실행
6. 다음 건으로 이동
7. 입력 결과 재검증
8. 교사가 최종 저장

학생 식별은 이름 하나가 아니라 가능한 경우 학년도/학년/반/번호/이름을 함께 사용합니다.

### 저장 정책

초기 자동입력은 **자동 채우기 + 사용자 검토 + 수동 최종 저장**을 기본값으로 합니다. 출결처럼 영향이 큰 데이터는 저장 버튼 자동 클릭을 별도 권한/설정으로 분리합니다.

## 4단계: 감시 레시피

체험학습 신청처럼 "새 행"을 찾는 기능은 목록의 전체 내용을 서버에 복제하지 않고 이전 상태의 최소 fingerprint와 비교합니다.

예:

```text
체험학습 신청 목록
  -> 신청번호/내부 행 식별자 수집
  -> 이전 fingerprint와 비교
  -> 새 식별자가 있을 때만 알림
  -> 앱에서 "나이스에서 확인" 실행
```

기본 저장값은 새 항목 판별에 필요한 최소 식별자와 마지막 확인 시각입니다. 학생명이나 신청 사유 같은 본문은 알림에 꼭 필요한 경우가 아니면 보관하지 않습니다.

## 5단계: 화면 학습

사용자가 직접 한 번 업무 흐름을 지정합니다.

```text
현재 화면 등록
  -> 이름 지정
  -> 화면 종류 선택
     - 바로가기
     - 자동입력
     - 새 항목 감시
  -> 필요한 필드/표 영역을 클릭
  -> 안정적인 DOM locator 후보 생성
  -> 사용자 확인
  -> 레시피 저장
```

가능하면 다음 순서로 locator를 선택합니다.

1. 안정적인 id/data-* 속성
2. 접근성 role/name
3. label과 input 관계
4. 표 header + row identity
5. 제한된 CSS selector

픽셀 좌표와 "Tab N회"만으로 구성된 레시피는 최후 수단으로 둡니다.

## 실패 안전성

자동화 실행은 다음 조건에서 중단합니다.

- 목표 페이지 식별 실패
- 학생/행이 0개 또는 2개 이상 매칭
- 날짜 열이 모호함
- 예상한 입력 컨트롤이 없음
- 페이지가 로그인 화면으로 바뀜
- 저장 전 검증 결과가 입력 계획과 다름

부분 실패 시 이미 입력한 항목과 실패 항목을 구분해 보여주고 조용히 다음 단계로 진행하지 않습니다.

## 단계별 구현 계획

| 단계 | 결과물 | 서버 영향 |
| --- | --- | --- |
| A | 로컬 사용자 정의 바로가기 | 없음 |
| B | 브라우저 확장 + 현재 화면 등록 | 없음 |
| C | DOM 레시피 엔진 + dry-run/검증 | 없음 |
| D | 학생 × 날짜 행렬 자동입력 | 없음 |
| E | 출결 앱 데이터 -> 나이스 입력 어댑터 | 기존 School Collect API만 사용 |
| F | 새 신청/결재 감시 + 로컬 알림 | 필요 시 최소 상태 API 검토 |
| G | AI 레시피 생성 보조 | 선택 기능, 실행 판단은 규칙 기반 유지 |

## 보안 경계

- Tauri 클라이언트는 신뢰 서버가 아닙니다.
- 학교 업무시스템 인증을 School Collect 서버로 프록시하지 않습니다.
- 브라우저 세션을 앱 서버로 전송하지 않습니다.
- 자동화 권한은 "사용자의 현재 로그인 세션에서 사용자가 할 수 있는 동작"을 넘지 않습니다.
- 실제 학교 화면 구조, URL, 캡처, 학생 데이터는 public 저장소/Issue/PR/로그에 넣지 않습니다.
- 화면별 레시피 공유 기능을 만들 경우 selector와 비민감 metadata만 공유하고 사용자 데이터는 분리합니다.

## 현재 미구현 범위

이번 첫 슬라이스는 다음을 아직 하지 않습니다.

- 현재 탭 자동 캡처
- DOM 클릭/입력
- Tab/Enter 매크로 실행
- 스포츠클럽/출결 화면별 adapter
- 체험학습 신규 신청 감시
- 로그인 유지 요청
- 인증서 자동 로그인
- AI 기반 화면 인식

이 기능들은 브라우저 동반 확장과 검증 가능한 레시피 실행기가 먼저 들어온 뒤 추가합니다.

## 후속 개선

첫 수직 슬라이스 리뷰에서 확인했지만 이번 수정에 넣지 않은 항목입니다.

- URL 검증: `url` crate(이미 workspace dependency)로 호스트 필수와 스킴 정규화를 적용합니다. 현재는 `https://:8080` 같은 값이 통과하고 `HTTPS://`는 거부됩니다.
- 실행 경계: `open_automation_target(url)`을 레시피 id 기반 명령으로 바꿔 렌더러가 임의 URL을 열지 못하게 합니다.
- 브라우저 열기: `tauri-plugin-opener` 또는 비동기 spawn으로 교체합니다. 현재는 `rundll32 url.dll,FileProtocolHandler`를 동기 실행하고 종료를 기다립니다.
- 레시피 파일 권한을 0600으로 좁히고 격리 파일 크기에 상한을 둡니다.
- 삭제 확인과 실행 취소, 레시피별 `aria-label`, 레시피 개수 상한.
- `decode_recipe`에서도 값을 trim합니다(현재는 검증만 trim).
- 레시피 범위는 School Collect 계정이 아니라 OS 사용자 단위입니다. 같은 Windows 계정으로 다른 학교 계정에 로그인하면 이전 사용자의 버튼이 보입니다.
