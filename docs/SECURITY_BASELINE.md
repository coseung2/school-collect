# 초기 보안 점검 기록

기준 SHA: `9ac77a84b100ac1a0a2c36ddfc1292410bff1582`. 기준일: 2026-09-21. 추적 이슈: #2.

## foundation 변경 범위

환경변수 예제의 값을 비우고, Supabase CLI 임시 상태와 TypeScript build cache의 추적을 제거합니다. 신규 환경파일·생성파일 및 일부 자격증명 패턴을 검사하는 guard를 추가합니다. 자격증명 값을 문서/로그/Issue에 재게시하지 않습니다.

## 완료되지 않은 일

전체 Git 이력 스캔, 원본 xlsx/docx/csv/pdf 등 업무자료 내용 검토, 개인정보 제거, 유효 credential 확인/무효화, 외부 사본 정리, 보호 설정 적용은 별도입니다. 이 PR은 기존 source document 전체를 삭제하지 않습니다. `.gitignore`는 기존 추적 파일과 과거 commit을 제거하지 않습니다.

Supabase 프로젝트는 사용자 확인에 따라 폐기된 것으로 취급하며 복구/재접속하지 않습니다. 이전 예제에 토큰 형식 값이 있었다는 사실만으로 현재 유효한 credential이라고 판단하지 않습니다. 다른 환경에서 재사용한 값인지 여부는 소유자가 확인해야 합니다.

## 보존 및 공개 범위

기존 main 및 과거 commit은 그대로 유지합니다. 보안 검토 전 새로운 공개 archive/tag를 생성하지 않습니다. 필요 시 접근 제한·이력 정리·보존 방법을 소유자가 결정하고, 백업·협업자 재동기화·GitHub 캐시/사본의 한계를 포함한 별도 절차로 처리합니다.

## 검사 한계

`check_repository.py`는 일부 명확한 패턴과 금지 경로만 검사합니다. binary/큰 파일/과거 이력·모든 비밀값을 검사하지 않습니다. 검사가 통과해도 저장소가 개인정보/비밀값 없이 안전하다는 보증은 아닙니다. 전체 이력 검증과 자료 분류가 끝나야 1단계를 완료합니다.
